apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "my-apache-chart.fullname" . }}-sm
  labels:
    release: my-monitor
spec:
  selector:
    matchLabels:
      app: apache
  endpoints:
    - port: metrics
      interval: 30s
