apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "my-apache-chart.fullname" . }}-config
data:
  status.conf: |
    <Location "/server-status">
        SetHandler server-status
        Require all granted
    </Location>
